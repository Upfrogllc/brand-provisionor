# Dependencies
node_modules/

# Secrets — never commit these
.env
*.env.local
config-*.json

# OS
.DS_Store
Thumbs.db

# The public/ and netlify/ folders MUST be committed
# (no ignore rules for them)
